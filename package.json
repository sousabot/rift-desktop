{
  "name": "rift-desktop",
  "version": "0.1.14",
  "description": "Rift.lol desktop app — League stats dashboard",
  "author": {
    "name": "Rift.lol"
  },
  "productName": "Rift.lol",
  "repository": "https://github.com/sousabot/rift-desktop",
  "main": "electron/main.js",
  "private": true,
  "scripts": {
    "dev": "concurrently -k \"vite\" \"wait-on http://localhost:5173 && node electron/dev-electron.js\"",
    "build": "vite build",
    "start": "electron .",
    "preview": "vite preview",
    "server": "node server/index.js",
    "dist": "node server/prepare-client-env.js && vite build && node scripts/electron-build.cjs",
    "dist:portable": "node server/prepare-client-env.js && vite build && node scripts/electron-build.cjs --portable",
    "release": "node server/prepare-client-env.js && vite build && node scripts/electron-build.cjs --release",
    "site:version": "node scripts/sync-site-version.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "electron-updater": "^6.6.2",
    "ffmpeg-static": "^5.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^8.2.2",
    "electron": "^31.3.0",
    "electron-builder": "^25.1.8",
    "rcedit": "^5.0.2",
    "vite": "^5.4.1",
    "wait-on": "^7.2.0"
  },
  "build": {
    "appId": "com.riftlol.desktop",
    "productName": "Rift.lol",
    "directories": {
      "output": "release"
    },
    "icon": "build/icon.ico",
    "files": [
      "dist/**/*",
      "electron/**/*",
      "package.json"
    ],
    "asarUnpack": [
      "**/node_modules/ffmpeg-static/**"
    ],
    "extraResources": [
      {
        "from": ".env.example",
        "to": ".env.example"
      },
      {
        "from": "client.env",
        "to": "rift.env"
      }
    ],
    "extraFiles": [
      {
        "from": "client.env",
        "to": "rift.env"
      }
    ],
    "forceCodeSigning": false,
    "afterPack": "./scripts/after-pack-icon.cjs",
    "win": {
      "icon": "build/icon.ico",
      "signAndEditExecutable": false,
      "verifyUpdateCodeSignature": false,
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "${productName}-${version}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "shortcutName": "Rift.lol",
      "artifactName": "${productName}-Setup-${version}.${ext}"
    },
    "portable": {
      "artifactName": "${productName}-${version}-portable.${ext}"
    },
    "publish": {
      "provider": "github",
      "owner": "sousabot",
      "repo": "rift-desktop",
      "releaseType": "release"
    }
  }
}
